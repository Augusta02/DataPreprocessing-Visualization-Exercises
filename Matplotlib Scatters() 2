import matplotlib.pyplot as plt
x = [1,2,3,4,5,6,7,8]
y =[4,1,3,6,1,3,5,2]


# scatter() is show the relationship between two variables x and y, s is used to increase or decrease the markers on the graph, c is used to assign a color to the markers on the graph, markers are used to change the shapes of the markers, alpha is used to change the transparency or hue of the color of the graph
plt.scatter(x,y,s=300,c="coral",marker="^",alpha=0.9)

# .title() is used to introduce a title for the graph
plt.title("Introduction to Matplotlib")

# .xlabel() and .ylabel() gives title to each axis
plt.xlabel('x')
plt.ylabel('y')

# .savefig() is used to save the file in png format
plt.savefig("Introduction_to_Matplotlib.png")

plt.show()
